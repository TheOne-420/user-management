import React from 'react';

function Loading() {
    return (
        <h2 className='font-extrabold text-4xl absolute top-[50%] left-[50%] -translate-[50] text-center'>
           Loading... 
        </h2>
    );
}

export default Loading;